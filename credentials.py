# For security, API Keys are stored in a seperate local file
# Avoid the hardcoding of sensitive credentials by assigning the api_key to a PATH variable
# or for a more secure option, retrieve credentials from a secrets manager (AWS Secrets Manager, Azure Key Vault, etc.)
API_KEY = "<api-key>"
